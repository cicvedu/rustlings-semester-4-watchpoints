// variables3.rs
//
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a
// hint.


fn main() {
    let x: i32 = 1;
    // 在c++中 默认值 int a;   cout << a 可能是 0 可能是其他很大值 但是不会编译报错

    println!("Number {}", x);
}
